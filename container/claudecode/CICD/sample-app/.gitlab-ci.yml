variables:
  MAVEN_OPTS: "-Dmaven.repo.local=./.m2/repository"

cache:
  paths:
    - ./.m2/repository/
    - backend/target/

before_script:
  - echo "🔧 Maven設定を準備中..."
  - mkdir -p ./.m2
  - |
    cat > ./.m2/settings.xml << 'XMLEOF'
    <?xml version="1.0" encoding="UTF-8"?>
    <settings xmlns="http://maven.apache.org/SETTINGS/1.0.0"
              xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
              xsi:schemaLocation="http://maven.apache.org/SETTINGS/1.0.0
              http://maven.apache.org/xsd/settings-1.0.0.xsd">
      <mirrors>
        <mirror>
          <id>nexus-mirror</id>
          <mirrorOf>*</mirrorOf>
          <url>http://cicd-nexus:8081/repository/maven-public/</url>
        </mirror>
      </mirrors>
    </settings>
    XMLEOF
  - echo "✅ Maven設定完了"

stages:
  - build
  - test
  - coverage
  - sonarqube
  - package
  - deploy

build:
  stage: build
  script:
    - mvn -B -e -fae -V -s ./.m2/settings.xml clean compile

test:
  stage: test
  script:
    - mvn -B -e -fae -V -s ./.m2/settings.xml test

coverage:
  stage: coverage
  script:
    - mvn -B -e -fae -V -s ./.m2/settings.xml jacoco:report

sonarqube:
  stage: sonarqube
  script:
    - echo "🔍 SonarQube 静的解析を実行中..."
    - echo "SonarQube Host ${SONAR_HOST_URL:-http://cicd-sonarqube:9000}"
    - echo "Project Key ${SONAR_PROJECT_KEY:-sample-org-management}"
    - |
      if [ -z "$SONAR_TOKEN" ]; then
        echo "❌ SONAR_TOKEN が設定されていません"
        echo "GitLab Settings → CI/CD → Variables でSONAR_TOKENを設定してください"
        exit 1
      fi
    - |
      # SonarQube接続テスト
      SONAR_URL="${SONAR_HOST_URL:-http://cicd-sonarqube:9000}"
      echo "🔗 SonarQubeサーバー接続テスト: $SONAR_URL"
      if ! curl -s -f "$SONAR_URL/api/system/status" > /dev/null; then
        echo "❌ SonarQubeサーバーに接続できません: $SONAR_URL"
        exit 1
      fi
      echo "✅ SonarQubeサーバー接続確認完了"
    - |
      # SonarQube解析実行
      echo "🔍 SonarQube解析実行中..."
      mvn -B -e -fae -V -s ./.m2/settings.xml sonar:sonar \
        -Dsonar.host.url="${SONAR_HOST_URL:-http://cicd-sonarqube:9000}" \
        -Dsonar.token="$SONAR_TOKEN" \
        -Dsonar.projectKey="${SONAR_PROJECT_KEY:-sample-org-management}"
    - echo "✅ SonarQube 解析完了"
    - echo "🌐 SonarQube Dashboard: http://cicd-sonarqube:9000/dashboard?id=${SONAR_PROJECT_KEY:-sample-org-management}"

package:
  stage: package
  script:
    - mvn -B -e -fae -V -s ./.m2/settings.xml package -DskipTests

deploy:
  stage: deploy
  script:
    - echo "🚀 Nexus Repository にデプロイ中..."
    - |
      if [ ! -f ".ci-settings.xml.template" ]; then
        echo "❌ .ci-settings.xml.template ファイルが見つかりません"
        exit 1
      fi
    - sed "s|__NEXUS_PASSWORD__|${NEXUS_ADMIN_PASSWORD}|g" .ci-settings.xml.template > .ci-settings.xml
    - NEXUS_URL="http://cicd-nexus:8081"
    - echo "Nexus URL $NEXUS_URL"
    - curl -f -s "$NEXUS_URL/service/rest/v1/status" > /dev/null || (echo "⚠️ Nexus Repository への接続に失敗しました" && exit 1)
    - echo "✅ Nexus Repository 接続確認完了"
    - mvn -B -e -fae -V deploy -DskipTests --settings .ci-settings.xml
    - echo "✅ デプロイ完了"
