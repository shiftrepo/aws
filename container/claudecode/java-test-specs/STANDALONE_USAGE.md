# スタンドアロン実行ガイド

このドキュメントでは、コンテナを使わずに実行可能JARファイルを使って、Java Test Specification Generatorをスタンドアロンで実行する方法を説明します。

## 必要な環境

- **Java 17以上** (JDK)
- 実行可能JARファイル: `java-test-specification-generator-1.0.0.jar`

## JARファイルの取得

### 方法1: リポジトリから取得

```bash
# リポジトリをクローン
git clone <repository-url>
cd java-test-specs

# ビルド（Maven必要）
mvn clean compile test package

# JARファイルの場所
# target/java-test-specification-generator-1.0.0.jar
```

### 方法2: ビルド済みJARを直接取得

```bash
# リポジトリのtarget/ディレクトリから
# java-test-specification-generator-1.0.0.jar をコピー
```

## 基本的な使用方法

### 1. ヘルプの表示

```bash
java -jar java-test-specification-generator-1.0.0.jar --help
```

### 2. 基本的な実行

```bash
java -jar java-test-specification-generator-1.0.0.jar \
  --source-dir ./src/test/java \
  --output test_specification.xlsx
```

**説明:**
- `--source-dir` : Javaテストファイルのあるディレクトリ
- `--output` : 生成するExcelファイルのパス

### 3. カバレッジレポートのディレクトリを明示的に指定

```bash
java -jar java-test-specification-generator-1.0.0.jar \
  --source-dir ./src/test/java \
  --coverage-dir ./target/site/jacoco \
  --output test_specification.xlsx
```

**説明:**
- `--coverage-dir` : JaCoCoカバレッジレポートのディレクトリを明示的に指定
- 省略した場合は、`--source-dir` 配下から自動検索

### 4. カバレッジ処理をスキップ

```bash
java -jar java-test-specification-generator-1.0.0.jar \
  --source-dir ./src/test/java \
  --output test_specification.xlsx \
  --no-coverage
```

### 5. デバッグモード

```bash
java -jar java-test-specification-generator-1.0.0.jar \
  --source-dir ./src/test/java \
  --output test_specification.xlsx \
  --log-level DEBUG
```

### 6. 対話モード

```bash
java -jar java-test-specification-generator-1.0.0.jar --interactive
```

対話的にパラメータを入力できます。

## 完全なワークフロー例

### ステップ1: テストとカバレッジレポートの生成

```bash
# Mavenでテストを実行してJaCoCoレポートを生成
mvn clean test

# JaCoCoレポートの確認
ls -lh target/site/jacoco/jacoco.xml
```

### ステップ2: テスト仕様書の生成

```bash
# 方法A: カバレッジディレクトリを明示的に指定
java -jar java-test-specification-generator-1.0.0.jar \
  --source-dir ./src/test/java \
  --coverage-dir ./target/site/jacoco \
  --output test_specification.xlsx

# 方法B: プロジェクトルートを指定（自動検索）
java -jar java-test-specification-generator-1.0.0.jar \
  --source-dir ./src/test/java \
  --output test_specification.xlsx
```

### ステップ3: 生成されたExcelファイルの確認

```bash
ls -lh test_specification.xlsx
```

## 別環境への移行

### 1. 必要なファイル

以下のファイルを別環境にコピーします：

```
java-test-specification-generator-1.0.0.jar  # 実行可能JAR
```

### 2. 別環境での実行

```bash
# Java 17以上がインストールされていることを確認
java -version

# テスト仕様書を生成
java -jar java-test-specification-generator-1.0.0.jar \
  --source-dir /path/to/test/java \
  --coverage-dir /path/to/jacoco/reports \
  --output /path/to/output/test_specification.xlsx
```

## コマンドラインオプション一覧

| オプション | 短縮形 | 引数 | 説明 |
|-----------|--------|------|------|
| `--source-dir` | `-s` | directory | Javaテストファイルのソースディレクトリ（必須） |
| `--output` | `-o` | file | 出力Excelファイルのパス（必須） |
| `--coverage-dir` | `-c` | directory | カバレッジレポートのディレクトリ（オプション） |
| `--no-coverage` | - | - | カバレッジレポート処理をスキップ |
| `--log-level` | - | level | ログレベル (DEBUG/INFO/WARNING/ERROR) |
| `--interactive` | `-i` | - | 対話モードで実行 |
| `--help` | `-h` | - | ヘルプメッセージを表示 |
| `--version` | `-v` | - | バージョン情報を表示 |

## トラブルシューティング

### エラー: Java version が古い

```bash
# Javaバージョンを確認
java -version

# Java 17以上が必要です
```

### エラー: カバレッジレポートが見つからない

```bash
# JaCoCoレポートが存在するか確認
ls -lh target/site/jacoco/jacoco.xml

# 存在しない場合はテストを実行
mvn clean test
```

### エラー: Javaファイルが見つからない

```bash
# ソースディレクトリのパスを確認
ls -lh src/test/java/

# 絶対パスを使用することも可能
java -jar java-test-specification-generator-1.0.0.jar \
  --source-dir /absolute/path/to/src/test/java \
  --output test_specification.xlsx
```

## 実行例

### 例1: 基本的な使用

```bash
$ java -jar java-test-specification-generator-1.0.0.jar \
    --source-dir ./src/test/java \
    --output my_test_spec.xlsx

📊 Java Test Specification Generator 開始
   バージョン: 1.0.0
   ソース: ./src/test/java
   出力: my_test_spec.xlsx
🔍 Step 1: Javaファイルスキャン開始...
✅ Javaファイル発見: 3個
📝 Step 2: アノテーション解析開始...
✅ テストケース抽出: 35個
📊 Step 4: Excelレポート生成開始...
✅ Excelレポート生成完了
============================================================
🎉 処理完了サマリー
============================================================
📁 Javaファイル処理: 3個
🧪 テストケース抽出: 35個
📈 カバレッジエントリ: 0個
⏱️ 処理時間: 1.5秒
📊 出力ファイル: my_test_spec.xlsx
📏 ファイルサイズ: 9,352バイト
============================================================
✅ テスト仕様書が正常に生成されました: my_test_spec.xlsx
```

### 例2: カバレッジ付き

```bash
$ mvn clean test  # まずカバレッジレポートを生成

$ java -jar java-test-specification-generator-1.0.0.jar \
    --source-dir ./src/test/java \
    --coverage-dir ./target/site/jacoco \
    --output test_spec_with_coverage.xlsx

📈 Step 3: カバレッジレポート処理開始...
   カバレッジディレクトリ: ./target/site/jacoco
✅ カバレッジデータ取得: 83個
...
📈 カバレッジエントリ: 83個
📏 ファイルサイズ: 15,115バイト
✅ テスト仕様書が正常に生成されました: test_spec_with_coverage.xlsx
```

## 出力されるExcelファイルの内容

生成されるExcelファイルには以下の情報が含まれます：

### Test Detailsシート（日本語項目）
- No.
- ソフトウェア・サービス
- 項目名
- 試験内容
- 確認項目
- テスト対象モジュール名
- テスト実施ベースラインバージョン
- テストケース作成者
- テストケース作成日
- テストケース修正者
- テストケース修正日

### Summaryシート
- 処理統計
- カバレッジ統計
- テスト実行結果
- 品質指標

### Coverageシート
- パッケージ、クラス、メソッド別のカバレッジ詳細
- Branch、Instruction、Line、Method各カバレッジ
- C1カバレッジ（条件判定カバレッジ）

### Configurationシート
- システム情報
- 処理設定

## よくある質問

### Q: JARファイルのサイズが大きい（約24MB）のはなぜですか？

A: すべての依存ライブラリ（Apache POI、JaCoCo解析など）が含まれた実行可能JARです。単一ファイルでどこでも実行できる利点があります。

### Q: カバレッジレポートなしで実行できますか？

A: はい、`--no-coverage` オプションを使用してください。

### Q: Windowsで実行できますか？

A: はい、Java 17以上がインストールされていれば実行できます。パスの区切り文字は `\` を使用してください。

### Q: 複数のプロジェクトを一度に処理できますか？

A: 各プロジェクトごとに個別に実行してください。

## サポート

問題が発生した場合は、`--log-level DEBUG` オプションで詳細ログを確認してください。

```bash
java -jar java-test-specification-generator-1.0.0.jar \
  --source-dir ./src/test/java \
  --output test_spec.xlsx \
  --log-level DEBUG
```
