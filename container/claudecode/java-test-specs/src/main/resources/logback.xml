<?xml version="1.0" encoding="UTF-8"?>
<configuration>

    <!-- コンソール出力設定 -->
    <appender name="CONSOLE" class="ch.qos.logback.core.ConsoleAppender">
        <encoder>
            <pattern>%d{yyyy-MM-dd HH:mm:ss.SSS} - %logger{36} - %level - %msg%n</pattern>
        </encoder>
    </appender>

    <!-- ファイル出力設定 -->
    <appender name="FILE" class="ch.qos.logback.core.rolling.RollingFileAppender">
        <file>test_spec_generator.log</file>
        <rollingPolicy class="ch.qos.logback.core.rolling.TimeBasedRollingPolicy">
            <!-- 日次ローテーション -->
            <fileNamePattern>test_spec_generator.%d{yyyy-MM-dd}.%i.log</fileNamePattern>
            <maxFileSize>10MB</maxFileSize>
            <maxHistory>30</maxHistory>
            <totalSizeCap>1GB</totalSizeCap>
        </rollingPolicy>
        <encoder>
            <pattern>%d{yyyy-MM-dd HH:mm:ss.SSS} - %logger{36} - %level - %msg%n</pattern>
        </encoder>
    </appender>

    <!-- パッケージ別ログレベル設定 -->
    <logger name="com.testspecgenerator" level="INFO" />
    <logger name="org.apache.poi" level="WARN" />
    <logger name="org.jsoup" level="WARN" />
    <logger name="com.fasterxml.jackson" level="WARN" />

    <!-- ルートロガー設定 -->
    <root level="INFO">
        <appender-ref ref="CONSOLE" />
        <appender-ref ref="FILE" />
    </root>

</configuration>