# PostgreSQL Client Authentication Configuration
# Allow connections from ANYWHERE without restrictions
# Security considerations disabled as per project requirements

# TYPE  DATABASE        USER            ADDRESS                 METHOD

# Local connections
local   all             all                                     trust
host    all             all             127.0.0.1/32            trust
host    all             all             ::1/128                 trust

# Allow connections from anywhere (0.0.0.0/0 = all IPv4 addresses)
host    all             all             0.0.0.0/0               trust

# Allow connections from any IPv6 address
host    all             all             ::/0                    trust

# Docker/Podman network connections
host    all             all             172.16.0.0/12           trust
host    all             all             10.0.0.0/8              trust
host    all             all             192.168.0.0/16          trust
